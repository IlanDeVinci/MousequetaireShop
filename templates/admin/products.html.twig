{% extends 'base.html.twig' %}

{% block title %}Admin - Products - Mousquetaire Shop{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {# admin listing styles moved to assets/styles/admin.css #}
{% endblock %}

{% block body %}
<div class="admin-page">
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <h2>Admin Panel</h2>
            <nav class="admin-nav">
                <a href="{{ path('app_admin_dashboard') }}" class="admin-nav-link">📊 Dashboard</a>
                <a href="{{ path('app_admin_products') }}" class="admin-nav-link active">📦 Produits</a>
                <a href="{{ path('app_admin_categories') }}" class="admin-nav-link">🏷️ Catégories</a>
                <a href="{{ path('app_admin_orders') }}" class="admin-nav-link">🛒 Commandes</a>
                <a href="{{ path('app_admin_users') }}" class="admin-nav-link">👥 Utilisateurs</a>
                <a href="{{ path('app_admin_banner_edit') }}" class="admin-nav-link">🎨 Bandeau</a>
            </nav>
        </aside>

        <main class="admin-content">
            <div class="admin-header">
                <h1 class="admin-title">Gestion des Produits</h1>
                <a href="{{ path('app_admin_product_create') }}" class="btn-create">✨ Créer un produit</a>
            </div>

            <div class="admin-table-container">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nom</th>
                            <th>Prix</th>
                            <th>Stock</th>
                            <th>Catégorie</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for product in products %}
                            <tr>
                                <td>#{{ product.id }}</td>
                                <td>{{ product.name }}</td>
                                <td>{{ product.price|number_format(2, ',', ' ') }} €</td>
                                <td>{{ product.stock }}</td>
                                <td>{{ product.category ? product.category.name : 'N/A' }}</td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="{{ path('app_admin_product_edit', {id: product.id}) }}" class="btn-edit">✏️ Modifier</a>
                                        <form method="POST" action="{{ path('app_admin_product_delete', {id: product.id}) }}" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce produit ?');">
                                            <button type="submit" class="btn-delete">🗑️ Supprimer</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="6" class="empty-state">Aucun produit trouvé</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>
{% endblock %}
