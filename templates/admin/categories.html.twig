{% extends 'base.html.twig' %}

{% block title %}Admin - Categories - Mousquetaire Shop{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {# admin listing styles moved to assets/styles/admin.css #}
{% endblock %}

{% block body %}
<div class="admin-page">
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <h2>Admin Panel</h2>
            <nav class="admin-nav">
                <a href="{{ path('app_admin_dashboard') }}" class="admin-nav-link">📊 Dashboard</a>
                <a href="{{ path('app_admin_products') }}" class="admin-nav-link">📦 Produits</a>
                <a href="{{ path('app_admin_categories') }}" class="admin-nav-link active">🏷️ Catégories</a>
                <a href="{{ path('app_admin_orders') }}" class="admin-nav-link">🛒 Commandes</a>
                <a href="{{ path('app_admin_users') }}" class="admin-nav-link">👥 Utilisateurs</a>
                <a href="{{ path('app_admin_banner_edit') }}" class="admin-nav-link">🎨 Bandeau</a>
            </nav>
        </aside>

        <main class="admin-content">
            <div class="admin-header">
                <h1 class="admin-title">Gestion des Catégories</h1>
                <a href="{{ path('app_admin_category_create') }}" class="btn-create">✨ Créer une catégorie</a>
            </div>

            <div class="admin-table-container">
                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nom</th>   
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for category in categories %}
                            <tr>
                                <td>#{{ category.id }}</td>
                                <td>{{ category.name }}</td>
                                <td>{{ category.description|default('N/A') }}</td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="{{ path('app_admin_category_edit', {id: category.id}) }}" class="btn-edit">✏️ Modifier</a>
                                        <form method="POST" action="{{ path('app_admin_category_delete', {id: category.id}) }}" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');">
                                            <button type="submit" class="btn-delete">🗑️ Supprimer</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4" class="empty-state">Aucune catégorie trouvée</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>
{% endblock %}
