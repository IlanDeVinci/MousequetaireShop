{% extends 'base.html.twig' %}

{% block title %}{{ category ? 'Edit' : 'Create' }} Category - Admin{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {# form styles moved to assets/styles/admin.css #}
{% endblock %}

{% block body %}
<div class="admin-form-container">
    <div class="form-card">
        <h1 class="form-title">{{ category ? 'Modifier' : 'Créer' }} une Catégorie</h1>

        <form method="POST">
            <div class="form-group">
                <label class="form-label" for="name">Nom de la catégorie *</label>
                <input 
                    type="text" 
                    id="name" 
                    name="name" 
                    class="form-input" 
                    value="{{ category ? category.name : '' }}" 
                    required
                >
            </div>

            <div class="form-group">
                <label class="form-label" for="description">Description</label>
                <textarea 
                    id="description" 
                    name="description" 
                    class="form-textarea"
                >{{ category ? category.description : '' }}</textarea>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn-submit">
                    {{ category ? '💾 Enregistrer' : '✨ Créer' }}
                </button>
                <a href="{{ path('app_admin_categories') }}" class="btn-cancel">Annuler</a>
            </div>
        </form>
    </div>
</div>
{% endblock %}
