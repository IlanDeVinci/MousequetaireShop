<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mousquetaire Shop - Accueil{% endblock %}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oxanium:wght@700&family=Montserrat:wght@700&family=Inter:wght@400&display=swap" rel="stylesheet">
    {% block stylesheets %}
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: white;
            overflow-x: hidden;
        }

        /* Barre d'info */
        .top-bar {
            background: #0e112b;
            height: 39px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-family: 'Oxanium', sans-serif;
            font-weight: 700;
            font-size: 14px;
            letter-spacing: -0.56px;
        }

        /* Header / Navigation */
        .header {
            background: #181e4b;
            height: 75px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 26px;
        }

        .header-content {
            display: flex;
            align-items: center;
            gap: 29px;
            width: 100%;
        }

        .logo {
            height: 47px;
            display: flex;
            align-items: center;
            text-decoration: none;
        }

        .logo img {
            height: 100%;
            width: auto;
            object-fit: contain;
        }

        .nav-button {
            background: white;
            height: 37px;
            padding: 0 20px;
            border-radius: 47px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 24px;
            color: #0b0e22;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: all 0.3s;
        }

        .nav-button:hover {
            background: #f0f0f0;
        }

        .nav-button.active {
            background: #0e112b;
            color: white;
        }

        .cart-icon {
            width: 54px;
            height: 54px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            margin-left: auto;
            padding: 10px;
            text-decoration: none;
        }

        .cart-icon img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        /* Alerts */
        .alert {
            padding: 15px 20px;
            margin: 20px;
            border-radius: 8px;
            font-family: 'Inter', sans-serif;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        /* Footer */
        .footer {
            background: #0a0d22;
            padding: 35px 165px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .footer-text {
            font-family: 'Inter', sans-serif;
            font-size: 18px;
            color: white;
            line-height: 1.215;
        }

        .social-icons {
            display: flex;
            gap: 20px;
        }

        .social-icon {
            width: 44px;
            height: 44px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            transition: all 0.3s;
            padding: 10px;
        }

        .social-icon:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .social-icon svg {
            width: 100%;
            height: 100%;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header-content {
                flex-wrap: wrap;
                gap: 10px;
            }

            .footer {
                flex-direction: column;
                gap: 20px;
                text-align: center;
                padding: 35px 20px;
            }
        }
    </style>
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
    <!-- Barre d'information supérieure -->
    <div class="top-bar">
        {% block topbar %}Ici, texte pour des offres ou infos du style : 🚚 Livraison offerte à partir de 100€ 🚚{% endblock %}
    </div>

    <!-- Header avec navigation -->
    <header class="header">
        <div class="header-content">
            <a href="{{ path('app_home') }}" class="logo">
                <img src="{{ asset('images/logo.png') }}" alt="Logo MouseQuetaire Shop">
            </a>
            <a href="{{ path('app_home') }}" class="nav-button {% if app.request.get('_route') == 'app_home' %}active{% endif %}">Accueil</a>
            <a href="{{ path('app_products') }}" class="nav-button {% if app.request.get('_route') == 'app_products' %}active{% endif %}">Magasin</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin_dashboard') }}" class="nav-button">Admin</a>
            {% endif %}
            {% if app.user %}
                <a href="{{ path('app_account') }}" class="nav-button">Mon Compte</a>
                <a href="{{ path('app_logout') }}" class="nav-button">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="nav-button">Connexion</a>
            {% endif %}
        </div>
        {% if is_granted('ROLE_USER') %}
            <a href="{{ path('app_cart') }}" class="cart-icon">
                <img src="{{ asset('images/panier.png') }}" alt="Panier">
            </a>
        {% endif %}
    </header>

    <!-- Messages flash -->
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-error">{{ message }}</div>
    {% endfor %}

    <!-- Contenu principal -->
    {% block body %}{% endblock %}

    <!-- Footer -->
    <footer class="footer">
        <p class="footer-text">
            Agence de développement web & design © {{ 'now'|date('Y') }} MouseQuetaire. Tous droits réservés
        </p>
        <div class="social-icons">
            <a href="#" class="social-icon">
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <rect x="2" y="2" width="20" height="20" rx="5" stroke="white" stroke-width="2"/>
                    <circle cx="12" cy="12" r="4" stroke="white" stroke-width="2"/>
                    <circle cx="18" cy="6" r="1.5" fill="white"/>
                </svg>
            </a>
            <a href="#" class="social-icon">
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M20 3H4C3.44772 3 3 3.44772 3 4V20C3 20.5523 3.44772 21 4 21H20C20.5523 21 21 20.5523 21 20V4C21 3.44772 20.5523 3 20 3Z" stroke="white" stroke-width="2"/>
                    <path d="M8 11V17M8 8V8.01M12 17V13M12 13V11M12 13C12 13 14 11 16 13V17" stroke="white" stroke-width="2" stroke-linecap="round"/>
                </svg>
            </a>
        </div>
    </footer>
</body>
</html>
